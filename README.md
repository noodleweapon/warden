# warden

This is a pure bash passwork keeper! I coded this before Claude Code existed, using my elite bash skills.

## ⚠️ This is a PUBLIC repo

Do **NOT** leak any secrets here. Never commit passwords, master passwords, real `*.md` key files, your `WARDEN_DIRECTORY` data, or anything else sensitive. Anything pushed to a public repo should be considered permanently exposed, even if later deleted.
